# job-aggregator

A scheduled pipeline that polls employer applicant-tracking-system job boards, deduplicates and filters the results against fixed rules, and writes what survives to a table for review.

**Status: the first slice is built and running.** Nine Greenhouse boards and two Lever boards are polled twice a day by GitHub Actions, and the results are written to the `data` branch of this repository. The review table (Airtable) is not built yet. `STATE.md` says exactly what exists, what has run, and what is still open.

## What this repository stores

On `main`: code, tests, decision records and documentation.

On the `data` branch, written by the scheduled run: job posting **metadata only** for employer boards. That is employer, title, location, ATS platform, publication date, first-seen date, stated experience, canonical URL, and the provenance fields later decision records require. Alongside them are a seen-posting store and one log per run. The `data-test` branch holds the same shapes from manual test runs.

## What it does not store

**Job description text, in any form.** No descriptions, no snippets, no raw HTML, no recorded API payloads containing description text. Test fixtures have description fields stripped before they are committed, and a pre-commit guard rejects any that are not.

This is a deliberate boundary, recorded in ADR-0011. Metadata is factual record-keeping. Employer-authored description text belongs to the employer, and republishing it is a different act from reading a public endpoint.

**Anything from job aggregators.** Their terms restrict redistribution, so their rows stay on the machine that fetched them and are never committed or pushed. The run refuses to commit a file that contains one. ADR-0020.

## What it does not do

No relevance scoring, ranking, or model-based classification of postings. Every posting is admitted or dropped by a deterministic rule that can be named. Reasoning in ADR-0010.

No user interface. No notification system. No paid data acquisition.

## Where to look

| You want | Read |
| --- | --- |
| What exists, what is blocked, and the proof | `STATE.md` |
| What each working session did | `logs/README.md` |
| Why the system is shaped this way | `docs/architecture-2.0.md` |
| Why a specific choice was made | `docs/decisions/` |
| What a file holds and when to open it | `MAP.md` |
| How to work in this repository | `CLAUDE.md` |

## Setup

Python 3.11 or later, and git. Once per clone:

```
git config core.hooksPath .githooks
python -m venv .venv
.venv/Scripts/python -m pip install -r requirements.txt      Windows
.venv/bin/python -m pip install -r requirements.txt          Linux and macOS
```

The only dependency is `requests`, installed inside `.venv`. Never install it globally.

The hook blocks a commit that stages a raw payload, an unsanitised test fixture, an Obsidian wikilink, implementation work without `STATE.md`, or a stale `MAP.md`. `MAP.md` is generated by `tools/generate_map.py` and must never be edited by hand; to change a row, edit that file's frontmatter and regenerate.

## Running it

The commands below use the Windows path to the environment's Python; on Linux and macOS use `.venv/bin/python`.

```
.venv/Scripts/python -m unittest discover -s tests -t .       every test
.venv/Scripts/python -m src.run --test-mode --no-commit       one run against the live boards,
                                                              written under data/test/, no commit
.venv/Scripts/python -m src.run --test-mode                   the same, committed to data-test
```

A local run writes its working files under `data/`, which git ignores. A committing run first loads the branch's stored state, so it appends to what the branch holds rather than to whatever this machine last saw.

**On GitHub**, `.github/workflows/fetch.yml` runs the tests and then the fetch at 00:00 and 13:00 UTC, and pushes the `data` branch. It can also be run by hand from the Actions tab; ticking "Write to alternate files, leaving production data alone" makes it a test run that writes `data-test` instead.

## Tools

| Command | What it does |
| --- | --- |
| `python tools/generate_map.py` | Regenerates `MAP.md`; `--check` reports whether it is current |
| `.venv/Scripts/python tools/run_log_report.py` | Requests per run and per source, stops, and silent boards, from the run logs on the `data` branch (ADR-0028) |
| `.venv/Scripts/python tools/publication_lag_report.py` | Each posting's publication date against the run that first saw it |
| `.venv/Scripts/python tools/mutate.py <file>` | Breaks the code in the ways a mutation file lists and confirms the tests notice |
| `python tools/make_cassette.py --source <saved response> --out tests/cassettes/<name>.json` | Builds a test fixture from a saved board response, with description text stripped |

The two report tools read the `data` branch, which is written on GitHub. Fetch it first with `git fetch origin data:data`, or pass `--repo` or `--dir`.

`docs/architecture.md` is a superseded design kept as a record of what was rejected. It carries a banner saying so. Do not implement from it.

## Contact

Waqas Sharif. If something in this repository should not be here, open an issue and it will be removed.

## Licence and scope

The code and documentation in this repository are licensed under the MIT
Licence. See `LICENSE`.

**That licence covers the code and documentation only.** Job posting metadata
is factual information about third-party job advertisements. It is not
authored by this project and is not licensed by it.

This project is not affiliated with, endorsed by, or connected to any employer,
applicant tracking system, or job board named anywhere in it. It reads publicly
accessible endpoints that those services publish for third-party use, at a low
rate, for one person's own job search.

The design deliberately keeps collected job data out of this repository.
Employer description text is never stored here in any form, and data from
sources whose terms restrict redistribution is never committed at all. See
ADR-0011 and ADR-0020 in `docs/decisions/`.

Anyone who forks this repository and uses it to collect, publish or
redistribute job data does so on their own responsibility and is bound by the
terms of whichever sources they poll, not by anything here.

Nothing in this repository is legal advice.
