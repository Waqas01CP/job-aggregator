# Session logs

One log per session, newest at the top. This table is the navigation index for all session history.

The first rows are diagnostic sessions. Implementation began with the vertical slice on 2026-09-17.

## How to use this file

Read this table first. It tells you what prior sessions did.

**When you need detail beyond what this table gives, read the most recent relevant log. That log references the one before it. Chain backwards only as far as you need, and stop when you have enough.** Do not read the directory.

This rule is what keeps the reading cost flat as the log count grows.

## How to maintain it

**Append your row in the same commit as your work.** A session that writes a log and does not index it has left the log invisible.

**Never delete or modify an existing row.** If a later session corrects an earlier one, that is a new row saying so.

**Rows are compressed. No prose, no reasoning, no elaboration beyond what a future session needs in order to decide whether to open the log.** The reasoning lives in the log, one level down. An index that explains is an index nobody finishes reading.

**If a log file exists with no row here, read it and add the row before doing anything else.**

## Log format

ADR-0024. In short: a header with date, model, the HEAD commit, and whether the session was read-only or mutating. Every factual claim carries a file and line, or a command and its output. Every claim tagged `[VERIFIED]` or `[BELIEVED]`, with unmarked meaning believed. Rejected alternatives recorded with the reason. What was checked and found already correct, recorded. What was not done, stated explicitly. Findings separated from decisions.

Length follows the work. No cap, no floor.

## Reorganisation

When this directory passes roughly 80 to 100 files at its root, split by lane into subdirectories and leave this index at the top pointing into them. Do it before the root becomes unscannable, not after.

## Index

| Date | Session | What was done | Outcome |
|---|---|---|---|
| 2026-09-22 UTC | Index correction, by the architecture chat | No log file; this row only. The eight rows from "The architecture chat's brief, worked through" up to "The operator's decisions, and a guard proved backwards" are sections of `logs/2026-09-17-corrections-and-airtable-schema.md`, from line 158, not files of their own. One section there had no row; it has one below | This table names no file, so a reader finds these by opening that log. Existing rows left as written, per the rule above |
| 2026-09-18 UTC, indexed 2026-09-22 | The run landed, and the prediction held | Section at line 277 of `logs/2026-09-17-corrections-and-airtable-schema.md`. Run 35303384355 verified: scheduled, at `bceb3d9`, every step succeeded; `data` moved `91f7518` to `90553c3`. First run on pool version 4 with the seniority rule | **0 rows reached the public filtered file:** the chain kept 294, every kept identity was already in the seen store, so the handoff's 241 was wrong and ADR-0030's backfill closes the gap. Seniority dropped 65. Himalayas kept 24 and lost all, third run running. Lever `createdAt` has a second observation. 36 requests every run |
| 2026-09-22 UTC, session close | The chat's files committed, and the close | `da193b6` committed `CHAT_STATE.md` and the decisions index on the operator's instruction, and answered its item 80: `STATE.md`'s headline named a commit eleven behind. `STATE.md` brought current against `0e0419f` and `data` at `137364f`. `docs/how-to/build-the-writer.md` assembles the held implementation's constraints from nine records | **ADR-0030's backfill is proven in production:** the 2026-09-19T03:26Z run reports `backfilled: 257`, the number measured by hand three days earlier, and all seven runs since report 0. The heredoc guard has never been observed firing; `.claude/` was absent at session start so the watcher never picked it up. Two anomalous runs on 2026-09-22 noticed, not investigated |
| 2026-09-18 UTC, same session, continued | The operator's decisions, and a guard proved backwards | Ten rows seeded into `Jobs test` as a canary before the writer. Backfill wired into every run, audited first. Retention split into two clocks. Contract check daily. Rozee.pk deferred. 383 tests | **The heredoc guard was backwards:** the escape is converted before the hook sees the command, so it fired on harmless regex backslashes and was silent on the failure. Retriggered on the destination. The trap fired a sixth time while the fix was being written. Airtable tables were empty because no code writes to them; the token unblocked building the writer, not writing rows |
| 2026-09-18 UTC, same session, continued | Three corrections from the operator | `CHAT_STATE.md` committed and the chat's two changes restored, after a seat reverted them instead of flagging. `tools/heredoc_guard.py` and a PreToolUse hook that asks only when a heredoc with a backslash writes a file. `docs/how-to/airtable-token-and-secrets.md`. 374 tests | **Reverting another seat's deliberate change was the error**, not the caution: a flag costs a message, an unexplained revert costs the decision. The guard asks and never denies, fails open, and half its 16 tests are cases it must not fire on. Seven secrets needed, not four |
| 2026-09-18 UTC, same session, continued | Classification flow, deferred register, and an audit | ADR-RULES audited. `docs/decisions/README.md` checked against every record. ADR-0045, the classification flow, supersedes ADR-0014 with the call budget recomputed. `docs/deferred/` created with a new map type. Five Airtable tables built and read back in the base the MCP now reaches | **The index says ADR-0010 has a clause reversed by 0038; it does not**, and ADR-0010 is the scope floor. **ADR-RULES adds two ranks to ADR-0022 and neither links to the other, while CLAUDE.md still carries the old eight.** **The MCP is on a different account:** the 2026-09-17 base is stranded. `CHAT_STATE.md` was set up for tracking by the chat and held back for the operator, the repository being public |
| 2026-09-18 UTC, same session, continued | STATE.md split by status, after the operator chose | Completes the row below, which left Part 5 proposed. 60 DONE rows moved verbatim to `docs/reference/completed.md`; `STATE.md` keeps the headline, Blocked, Known unverified and the nine rows that are not DONE | 42,036 bytes to 23,560. Verified nothing was lost: 69 rows before, 9 plus 60 after, sets equal. ADR-0023's artifact set gains a file; the reading order is unchanged |
| 2026-09-18 UTC, same session, continued | Outcome stores, the backfill, and the PAT | `tools/backfill.py` built and run: 257 rows appended, gap closed, Confirmation proved able to fail by withholding an identity. `src/star.py`, the deterministic priority star. `load_term_families` maps all 79 terms to their family, which ADR-0038 needed and `load_title_pool` discarded. ADR-0043 three outcome stores, ADR-0044 the star. 358 tests, 13 mutations all caught | **The PAT is now a blocker in `STATE.md`, not an unbuilt item:** the writer, the sweep, the stores and the star all sit behind it. ADR-0043 reverses ADR-0014's one-file clause and keeps its invariant as a checkable property. **STATE.md's seam is status:** 57 of 69 rows are DONE and are 45% of the file. Proposed, not built |
| 2026-09-18 UTC, same session, continued | Navigation built, after the operator chose | Completes the row below, which left navigation proposed and not built. All 41 records given `topic` and `description`; `MAP.md`'s decision table grouped under six topic headings in pipeline order; the generator gained a gate refusing a record with no topic or an unknown one | The operator chose grouping and descriptions together, and to push when a brief asks for it. Gate proven by stripping a topic and watching the map check exit 1. 325 tests |
| 2026-09-18 UTC, same session, continued | Navigation, storage, and an architecture audit | ADR-0031's Confirmation run for the first time and made a tool, `tools/preference_audit.py`, with 14 tests. `docs/reference/platform-fields.md` built from the 84 saved responses. ADR-0040, current rules filter the projection. ADR-0041, location admits unless a source excludes. Himalayas stays. 325 tests | **No module hard-codes a preference and no employer name appears in any module**; 2 violations found and fixed, both one identifier. **Himalayas returns `seniority` on 100% of postings and nothing reads it.** Its `locationRestrictions` is read, not ignored, and the truncation costs 2 of 74 postings. 11 of 24 filtered rows are rejected by today's chain. Navigation investigated and proposed, not built |
| 2026-09-17 UTC, same session as the row below, continued | The architecture chat's brief, worked through | Ten records, ADR-0030 to ADR-0039, answering questions 5, A, B, D, F, G, I and J and resolving ADR-0019's contradiction. Fifteen Changes rows and four in-place annotations across eleven records. Annotation vendors moved from a constant to a loaded config file. Hook gate 6 added for `data/` paths. 311 tests, 4 mutations all caught | **Two corrections to the brief:** ADR-0021's Confirmation is not invalidated, since all eight cases still produce their required verdict and only the mechanism changed for "Software Engineer II"; and ADR-0023's correction was already done on 2026-09-17. Gate 6 proven to fire against `git add -f`. Nothing pushed: the brief says push, the protocol says the operator does |
| 2026-09-17 UTC, after the row below | Corrections, a non-retroactive pool, and the Airtable schema | Four stale `STATE.md` push lines corrected, and the row below's test count: the suite reports 308, not 311. UTC dating rule added to `CLAUDE.md`, which is why this row's date precedes the one below it. Airtable authorized by the operator; `Jobs` and `Jobs test` built and read back. Baseline taken from a scratch clone of `data` | **A pool widening is not retroactive:** `src/run.py:178` writes only identities new to the seen store, so 257 of the 270 rows the current chain keeps can never reach `filtered.json`, the 241 Speechify copies among them. The handoff's "expect 241 on the next run" is wrong. Raised for the chat. Airtable MCP cannot delete a field or create a view |
| 2026-09-18 | Pool version 4, and two blocked items | Eighteen AI terms added ahead of the boards that carry them; bare `research engineer` rejected; the gerund gap class named in the pool. 311 tests, 4 mutations, one weak test strengthened | `ai engineering` immediately admitted a Veeam AI internship version 3 dropped. Himalayas' roles cannot be seen from GitHub at all, only counts; options recorded. Airtable tools reach only a new session |
| 2026-09-17 | The first scheduled run | Verified run 35253312417 from the public API and a scratch fetch of `data`. Ran the run-log and publication-lag reports on two production runs | Restore, append and fast-forward push all proven on GitHub; run 1's records untouched. Himalayas first contact on every run. 8 of 8 new postings dated after the previous run. Two new Joblogic roles in Pakistan. Rows kept before the seniority rule stay in the filtered file; raised |
| 2026-09-17 | Role families, seniority, and two trials | Pool version 3 in the operator's family order with ten software terms. Seniority rule decided by the operator, II and III checked against Veeam descriptions, architect kept. ADR-0021's case set tested for the first time. Himalayas and Banyan Canopy on trial until 2026-09-24. 303 tests, 13 mutations | On production data: 267 kept, 241 of them Speechify city copies, 32 dropped for seniority, 6 mentioning Pakistan. The rule departs from ADR-0021's no-blocklist and its "Software Engineer II" case; raised. A false note about `software engineer i` corrected. Not pushed |
| 2026-09-17 | The operator's decisions D1 to D5 | Moved `data/test` to `data/archive/2026-09-16-test`, verified. Pool version 2 with `forward deployment`; the 39 unmatched terms kept and recorded. `tools/title_pool_report.py` built and 32 candidates previewed. Experience rule deferred. Himalayas evidence gathered from saved responses. Airtable plugin examined. 285 tests, 16 mutations | Himalayas: 74 of 91 saved postings exclude Pakistan, one of 8 pool matches fits the operator's levels. A senior-word title rule would drop 10 of 21 kept. Found and removed a stray `data/seen.json` written by my own earlier test |
| 2026-09-17 | Test run verified, and the work that needed no input | Ticked run 35239033514 succeeded: `data-test` pushed, `data` untouched. Built `tools/publication_lag_report.py` for Lever `createdAt`. README brought up to date. ADR-0023 annotated. Decision pack written for the operator, local only. Greenhouse metadata checked for the experience rule. 268 tests, 14 mutations | Test mode works on GitHub; the restore path is still unexercised there. Airtable writer and sweep wait on the chat: the shared HTTP module must change. Gate 4's date is 2026-09-15, not 09-11. The metadata lead is weak: one board, a grade code, no years |
| 2026-09-17 | The first manual GitHub runs | Operator pushed `6476573` and ran the workflow twice. Ticked run failed its tests: job-level `TEST_MODE=1` reached the test step. Fixed in the tests and the workflow, reproduced by an extended simulation that now runs the test step. Production `data` branch checked. 252 tests, 5 mutations | Unticked run succeeded and created public `data`: Greenhouse and Lever only, 36 requests, Himalayas' 21 kept rows lost with the runner. Speechify's 191 is the 09-16/09-17 intersection. Fix not pushed |
| 2026-09-17 | Run-log reader, Node 24 actions, Speechify | The four no-input follow-ups. Actions moved to checkout v5 and setup-python v6. Em-dash count corrected to 54 in 7 files, one prose instance fixed. `tools/run_log_report.py` built for ADR-0028. Speechify's loss explained from saved data. Local title-pool review file written for the operator. 249 tests, 22 more mutations, one survivor fixed | Speechify lost four whole roles, 723 postings; the rest is location rotation. 361 to 191 unexplained. GitHub disables scheduled workflows after 60 days without repository activity, raised. Nothing pushed |
| 2026-09-17 | A runner-safe data branch | Found the first GitHub run: scheduled, fetched 1239, failed at `commit-tree`. Fixed five defects: no git identity, aggregator records inside the committed filtered and seen files, no state restored on a runner, test mode pushing production's branch, a text-mode git boundary on Windows. Mutation harness rebuilt in `tools/`. Verify-do-not-trust rule added to `CLAUDE.md`. 225 tests, 39 mutations, one survivor fixed | Old and new code both run through a local simulation of the workflow's steps. Nothing pushed, so the fixes have not run on GitHub. Himalayas keeps nothing on a runner, against ADR-0020's premise; raised for the architecture chat |
| 2026-09-17 | The vertical slice, built | Config, HTTP module, both adapters, normaliser, deduplicator, filter chain, writers, orchestrator, schedule, plus Himalayas on condition. 183 tests, 79 mutations, 8 survivors all resolved | Four live runs: 917 postings, 19 kept, second run byte-identical. Speechify collapses to 11 keys not 8, which is ADR-0001's key doing its job. Two filter rules have no definition anywhere and are not invented. Himalayas passes ADR-0019 on its three named components and fails its "config and adapter only" wording. Log carries a session-close section: why 8 mutations survived, the `_git` default-argument defect, what was tried and failed, 9 things noticed and not investigated |
| 2026-09-16 | Second-observation checks on three date fields | 7 GETs of a self-imposed 12. Tests chosen to need no elapsed time, since only 1h31m had passed | Workday `startDate` confirmed behaviourally, 7 of 7 across ages 1 to 13 days. The iCIMS "generated timestamp" suspicion is **withdrawn**: an older posting reads 2025-05-15. Lever `createdAt` still unsettled on one appearance in five days |
| 2026-09-16 | Publication date across the 13 untested platforms | 53 GETs of a 60 cap, one board per platform. Item 54: both earlier logs retyped `research` to `log`, map regenerated | Zero NO ENDPOINT. PROCEED: Ashby, Workable, SmartRecruiters, Breezy, Pinpoint, BambooHR. INVESTIGATE: the other 7. Manatal exposes no date field on 34 postings across 2 boards, which is ADR-0007's material case. Workday and BambooHR need 1 request per posting for a date |
| 2026-09-15 | Spike follow-up checks | 4 checks: 2 on disk, 10 GETs (Lever pages, Himalayas). Backfilled the 2026-09-11 log. STATE rows updated | 1646 postings on 11 boards falsifies ADR-0001:27 and ADR-0003:28. Speechify floor real: 8 titles across 329 locations. Lever pages show no date. Himalayas browse paginates newest-first; search does not |
| 2026-09-11 | Endpoint feasibility spike, log written 2026-09-15 | 13 GETs: Greenhouse 9 boards, Lever 2, Himalayas 1 | Greenhouse PROCEED, Lever INVESTIGATE (no employer field), Himalayas PROCEED. Publication date on 100% of postings on all three |
