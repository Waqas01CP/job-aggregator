---
type: state
description: What exists right now, what is blocked and on whom, and where the proof is. Where to start, then checked against the code and data.
status: current
---

# STATE

**Touched 2026-09-24T16:33Z by the implementing seat, for `CLAUDE.md`'s briefs rule only:** the operator refined it: addenda append while a brief is unexecuted, the old brief is read before the next replaces it, and every seat gets one file. Nothing else changed.

**Touched 2026-09-24T16:13Z by the implementing seat, for the briefs folder only:** `briefs/` holds the latest brief to each other seat, gitignored and skipped by the map, with `CLAUDE.md` naming the rule; a new test proves the map skips `data/`, `raw_responses/` and `briefs/`, and was made to fail by un-skipping `briefs/`. No pipeline code changed. The line that follows stands.

**Last verified 2026-09-24T15:30Z by the implementing seat, for the operator's `Status` rename, against `main` at `a9a59d3`.** Checked for it: both tables' `Status` choices and every marked row read through the connector, and each row's `Classified at` compared with its value read at 15:08Z. `2026-09-24-operator-decisions-and-the-next-builds.md`. The line that follows is the previous one.

**Touched 2026-09-24T15:12Z** *(stamped 15:16Z when written, four minutes ahead of the clock; pre-commit gate 4 reads only a "Last verified" line, so it did not catch it. Corrected in the next commit)* **by the implementing seat, for one log correction only:** the log's D5 row claimed the operator answered D5, when it came from his go on G1 to G6. Nothing was re-verified for it; the line that follows stands.

**Last verified 2026-09-24T15:10Z by the implementing seat, for the first live write to the private store and the `Status` choices, against `main` at `e2a481d` and `data-test` at `fe8f56f`.** Checked for it: test-mode dispatch 36016481510 through the public Actions API and its committed run log from a scratch clone of `data-test`; every stored file on `data-test` searched for Himalayas; both tables' `Status` choices and every marked row read through the connector. `2026-09-24-operator-decisions-and-the-next-builds.md`. The line that follows is the previous verification.

**Verified earlier, 2026-09-24T14:15Z by the implementing seat, for G6, against `main` at `1187193`, pushed.** Checked for it: 542 tests on Python 3.12 and 3.11; eleven contract-check mutations, all caught; the check run twice against the live boards with `--no-commit`, then once more with a field removed by hand from the stored fingerprint, which it named. `2026-09-24-operator-decisions-and-the-next-builds.md`. The line that follows is the previous verification.

**Verified earlier, 2026-09-24T13:44Z by the implementing seat, for G2 and D7, against `main` at `101371d`.** Checked for it: 517 tests on Python 3.12 and 3.11; thirteen private-store mutations, all caught; the three test classification tables read back from the connector after creating them. `2026-09-24-operator-decisions-and-the-next-builds.md`. The line that follows is the previous verification.

**Verified earlier, 2026-09-24T12:50Z by the implementing seat, for G4, against `main` at `d2000cf`.** Checked for it: the `Family` field created on `Jobs` and `Jobs test` through the connector; 501 tests on Python 3.12 and 3.11; six Family mutations, all caught. `2026-09-24-operator-decisions-and-the-next-builds.md`. The line that follows is the previous verification.

**Verified earlier, 2026-09-24T11:38Z by the implementing seat, for G3, D4, G1 and D1, against `main` at `1624c7e`.** Checked for it: the connector's answer on both `Title` descriptions; every stored title folded both ways before the fold changed; 500 tests on Python 3.12 and 3.11. `2026-09-24-operator-decisions-and-the-next-builds.md`. The line that follows is the previous verification.

**Verified earlier, 2026-09-24T11:27Z by the implementing seat, for the operator's decisions of that day, against `main` at `df7204e`.** Checked for it: the connector's field-update schema; each record line re-read before its annotation; the new status gate given the audit's misspelling; the suite free of ResourceWarnings; `Jobs` read for the duplicated Motive row; GitHub's documentation on the 60-day shutoff. `2026-09-24-operator-decisions-and-the-next-builds.md`. The line that follows is the previous verification.

**Verified earlier, 2026-09-24T10:11Z by the implementing seat, after the audit of `d8f3658` to `f864053` and production's first projection, against `main` at `f864053`, `data` at `a5abc3b` and `data-test` at `d71e4ea`.** Checked for it: scheduled run 35951490496 through the public Actions API and its committed run log from a scratch clone of `data`; `Jobs` read back through the connector; each audit finding reproduced or read at its line before it was fixed; the suite on Python 3.12 and 3.11; the new mutations; and the new STATE.md timestamp check made to fire. `2026-09-23-the-projection.md`, fourth section. The line that follows is the previous verification.

**Verified earlier, 2026-09-23T20:44Z** *(stamped 20:50Z when written, six minutes after the commit that carried it, `f864053` at 20:44:43Z; corrected 2026-09-24 after the audit, which found it)* **by the implementing seat, after live test run 35917058573, against `main` at `af899c1`, `data` at `bab0acf` and `data-test` at `d71e4ea`.** Checked for it: the run through the public Actions API; both test runs' committed logs from a scratch clone of `data-test`; `Jobs test` and `Jobs` through the connector, including the four hand-set `Status` values and their `Classified at` times; and a plan over `data-test` confirming two of the marked rows were re-sent. The line that follows is the previous verification, kept as written.

**Verified earlier, 2026-09-23T20:17Z** *(stamped 20:30Z when written, after the commit that carried it, `af899c1` at 20:17:16Z; corrected 2026-09-24 after the audit. The same line's "the new title and hook mutations" was also wrong: there was one mutation, for the title, and the hook was proved only by a scratchpad probe. A repository test of the launcher and its mutations were added on 2026-09-24)* **by the implementing seat, after live test run 35912554579, against `main` at `5768cf0`, `data` at `bab0acf` and `data-test` at `105d10a`.** Checked for it: the run through the public Actions API; its committed run log from a scratch clone of `data-test`; `Jobs test` and `Jobs` through the connector; the suite, and the new title and hook mutations, named in `2026-09-23-the-projection.md`. The line that follows is the previous verification, kept as written.

**Verified earlier, 2026-09-23T18:35Z by the implementing seat, for Brief 6, against `main` at `ee0fc79` and `data` at `bab0acf`.** Checked for it: `origin/main` at `b242d9a` at 17:50Z, with the architecture chat's rulings then uncommitted in the tree and committed as `ee0fc79`; `Jobs` empty and `Jobs test` holding the ten seeded canary rows, read through the connector; Airtable's upsert and rate limits read from its own documentation; the suite and the mutation results named in `2026-09-23-the-projection.md`. The line that follows is the previous verification, kept as written.

**Verified earlier, 2026-09-23T12:40Z by the implementing seat, against `main` at `d8f3658` and `data` at `def4f93`.** Checked this session: the push state (`git ls-remote --heads origin` put `main` at `8f4c251` at 12:04Z, so `d8f3658` and the step-1 commit are the unpushed ones), **427 tests passing on Python 3.12 and 3.11**, ADR-0034's mutation Confirmation, `MAP.md` current, six hook gates enabled, the Actions history (15 runs, 13 succeeded, the 2 failures both on 2026-09-17), the thirteen run logs on `data`, and the Airtable `Status` and `Classified at` fields through the connector. Everything else below carries the date of its own verification. `2026-09-22-speechify-rotation-and-the-writer-check.md`.

**Revised by the architecture chat against origin, 2026-09-22 (UTC).** `main` at `da193b6` and `data` at `7bb4c76`, by `git ls-remote --heads`; the eleven production run logs on `data` read directly from a clone at that head; the decision directory listed; the Airtable schema read through the connector. **Not rechecked, carried from the seat's line below:** the test count, `MAP.md`'s currency, the pool's term count, and every row not named in this revision. *(Corrected 2026-09-22T20:56Z by the implementing seat: the two commits this line names have both moved. `main` is at `0e0419f` and `data` at `137364f`, and the working copy is clean, so the sentence about uncommitted edits is no longer true: they were committed in `807add2`, `a165d15`, `10b3273` and `0e0419f`. The run count has also moved from eleven to twelve.)* This line and the seat's line below are the only places this file names a commit.

**Last verified against `main` at `0e0419f`, 2026-09-22T20:56Z**, for the push state, the suite's test count, the board count, the pool's term count, `MAP.md`'s currency and the Actions run history. Checked by `git ls-remote --heads origin`, the public Actions API, `python -m unittest discover`, and loading the pool. Four lines that claimed work was unpushed were stale and are corrected below. Also verified this session: the Airtable schema, read back after building it; that a pool widening is not retroactive; the record count, 39 *(wrong when stamped: the corpus held 45 records at that time per the seat's own Addendum 10, and holds 48 on 2026-09-22, 47 numbered plus ADR-RULES, by directory listing)*; that ADR-0021's eight Confirmation cases all still produce their required verdict; and that pre-commit gate 6 blocks a force-added `data/` path. Dates in this file are UTC from 2026-09-17, per CLAUDE.md. Verified in the navigation and audit round: ADR-0031's Confirmation now runs as a tool and the shipped tree is clean; 325 tests; the field inventory is measured from 84 saved responses; run 35303384355 wrote 0 rows to the public filtered file; the map's topic gate, which fires on a record with no topic; the backfill, run against a copy of the branch, which appended 257 rows and closed the gap; the five Airtable tables, each schema read back after building; that both Airtable tables were empty before ten rows were seeded into `Jobs test`; and that the heredoc guard's first design was blind to the failure it was built for.

**Touched again 2026-09-22T22:10Z by the implementing seat, after the close, for `docs/how-to/the-seats.md`.** No pipeline code changed and no test was run: the change is documentation only. Verified for it: six gates in `.githooks/pre-commit`, `core.hooksPath` returning `.githooks`, 42 Python files and 8,642 lines, 49 files in `docs/decisions/`, the four reading-order files at 72,765 bytes, and `MAP.md` current at 87 files. **`origin/main` is at `73fff5c`** [VERIFIED] `git ls-remote --heads origin`, 2026-09-22T22:15Z, **so the close commit is pushed and only this one is not.** *(This sentence first read "`origin/main` at `0e0419f`, so two commits are now unpushed". Wrong when written: it was carried from a session summary rather than checked against the remote, which is the failure `CLAUDE.md` names first. The operator pushed `73fff5c` between the close and this commit. Corrected in the same session, minutes later, by running the command.)* One claim this seat made to the operator was wrong and is corrected in the log: the seat protocol does exist in a repository file, `CHAT_STATE.md`, in six lines addressed to the architecture chat.

Verified earlier, against `main` at `085c434`, 2026-09-17, and not rechecked since: the Known unverified entries dated 2026-09-17, the README row, the title pool and seniority rows, the Himalayas and Banyan Canopy trials, and the Tooling rows added on 2026-09-17. Other Documentation and Tooling rows are carried forward from earlier verifications and were not rechecked.

This file is where to start, not where to stop. It outranks memory: if you believe a row is wrong, read the file the pointer names before claiming a conflict. It does not outrank the code or the data. Where a row disagrees with them, the row is stale; report it and correct it. On 2026-09-17 four stale lines were found here that way.

Organised by task, not by session, because logs are chronological and one task spans many of them.

## How to read a row

`STATUS` is DONE, PARTIAL or PENDING. A row reads DONE only when the thing is built, never when it has merely been examined.

Every row carries `[VERIFIED]` or `[BELIEVED]`. Verified means exercised and observed. Believed means reasoned from the code but not run. **Unmarked means believed.** The default points at the weak set on purpose.

`Proof` is a commit, a decision record, or a log filename. Never a file path; paths move.

## How to maintain it

Update the affected rows **in the same commit as the work**, never in a separate pass. A commit that completes a task without updating its row is incomplete.

**When a row becomes DONE, move it to `docs/reference/completed.md` in the same commit.** Never delete one. Supersede it with a new row there if the work is redone. This file holds what is not settled; that one holds what is.

Update the verified-against line whenever you touch this file.

## Headline

**The vertical slice runs on schedule on GitHub.** [VERIFIED] 2026-09-23T12:09Z from a clone at `data` head `def4f93`: **thirteen** production runs from 2026-09-17T14:52Z to 2026-09-23T03:36Z, every one spending 36 of 500 requests with no retry and no failure. `filtered.json` holds **345** rows, the seen store **927**, raw Greenhouse 880 and Lever 47. Test mode works on GitHub on `data-test`.

**The two anomalous runs of 2026-09-22 were Speechify rotating its city copies, not a board fault.** [VERIFIED] 64 copies left at 03:36Z and 62 different ones arrived at 17:33Z under old publication dates; every board parsed cleanly on every run. 60 of them entered `filtered.json`, which now holds **301 copies of one Speechify role in 345 rows**. `written_filtered` counts public and local rows together, so the public file received nothing in six of the seven runs before 17:33Z. `2026-09-22-speechify-rotation-and-the-writer-check.md`.

**ADR-0030's backfill is proven in production, and this is the single most useful thing a new seat can know about it.** [VERIFIED] from the run logs: the 2026-09-19T03:26Z run reports `backfilled: 257`, which is exactly the number measured by hand three days earlier, and **every one of the seven runs since reports 0**. It appends the gap once and nothing thereafter, which is the idempotence the design claimed and the reason it is safe inside every run.

**The display fills on every scheduled run.** [VERIFIED] scheduled run 35951490496, 2026-09-24T03:27Z, the first production projection: 360 rows read (345 public, 15 Himalayas), 349 admitted, 44 groups, 44 sent in 5 calls, no failure; `Jobs` read back through the connector holds 44 records. ADR-0009's slice Confirmation now waits only on the operator finding in `Jobs` a role he had not already seen by hand. *(Until 2026-09-24 this paragraph read "The display does not fill yet", with the writer held; the audit of that date found it stale.)*

**Himalayas' kept rows are lost on every run.** *(2026-09-24: ADR-0047's write path is built and offline-verified: a committing run restores the aggregator files from the private repository's `data` or `data-test` branch and pushes them back. Not yet run live; see Blocked.)* [VERIFIED] each of the thirteen runs to 2026-09-23T03:36Z fetched 500 and kept between 11 and 31, and every kept row was discarded with the runner. ADR-0047 gives them a private store; it is decided and unbuilt, and needs the operator's step 5.

**Committed 2026-09-22, and this paragraph is kept only to say so.** It listed the architecture chat's edits of 2026-09-19 to 2026-09-22 as uncommitted and awaiting the implementing seat. They are in `807add2`, `a165d15`, `10b3273` and `0e0419f`: ADR-0046, ADR-0047 and ADR-0048, the Changes-table move across sixteen records, the rewritten `CLAUDE.md` authority order, `docs/architecture-2.0.md`, both reference files, the how-to, and this file. [VERIFIED] `git status` clean and `origin/main` at `0e0419f`.

Four spikes have run and their findings are folded into the records. The four decisions they raised are answered by ADR-0028 and ADR-0029.

---

**The 62 completed rows live in `docs/reference/completed.md`**, read on demand rather than at the start of every session. A DONE row is finished history: it records that something was built and how it was proved, and under this file's own rule it is never deleted. Keeping them here meant every session read 18.7KB of settled work to reach the nine rows that are not settled: 60 of the 69 rows were DONE, and the file went from 42,036 bytes to 23,334. Nothing was rewritten in the move.

## Documentation

| Task | Status | Evidence | Date | Proof |
|---|---|---|---|---|
| End-state document | PENDING | — | — | Confirmed open in ADR-0023. No prior art exists |

## Tooling

| Task | Status | Evidence | Date | Proof |
|---|---|---|---|---|

## Pipeline

| Task | Status | Evidence | Date | Proof |
|---|---|---|---|---|
| Spike follow-up: board volume, Speechify age floor, Lever `createdAt`, Himalayas pagination | PARTIAL | [VERIFIED] checks 1 to 3 complete. Check 4 complete except whether browse pagination terminates, not establishable inside its 7-request cap | 2026-09-15 | `2026-09-15-spike-followup-checks.md` |
| Himalayas adapter | PARTIAL | [VERIFIED] built, tested and run live: cursor pagination, stop anchored on stored data, aggregator rows routed to a local file never committed. **It stays**, decided 2026-09-18; ADR-0039 settled that ADR-0019's three named components govern, not its narrower wording. **On trial to 2026-09-26.** [VERIFIED] every production run so far fetched 500 and kept 11 to 28, all discarded with the runner, so the trial has no stored rows to judge. **Decided, unbuilt:** a private store for every aggregator-sourced layer and the private Airtable base (ADR-0047) *(the private store built 2026-09-24, offline-verified, waiting on its first live run)*, and one poll a day on the morning run (ADR-0048) *(ADR-0048 built 2026-09-24: `poll_slots` in `config/boards.json`, the workflow naming the slot from its cron, and the evening run logging Himalayas as skipped. Its Confirmation waits on the first evening run after the push)* | 2026-09-22 | `2026-09-17-vertical-slice.md`, ADR-0039, ADR-0047, ADR-0048 |
| Filter chain | PARTIAL | [VERIFIED] expiry and title built and tested, 34 tests, 10 mutations all caught. **Experience is disabled: no record names a threshold and no slice platform returns the field**, and the operator has deferred the rule until filtering reads descriptions. **The annotation-vendor list is no longer provisional in code:** it moved from a constant in `src/filters.py` to `docs/reference/annotation-vendors.md` with a loader, per ADR-0031, with the three names unchanged. Its evidence is still one census of 34 rows and it has never fired on this pipeline's boards | 2026-09-17 | `2026-09-17-vertical-slice.md`, ADR-0005, ADR-0021 |
| Airtable writer | **PARTIAL: steps 1 and 2 built, live, and audited; the star and the sweep remain** | **Production, scheduled run 35951490496, 2026-09-24T03:27Z** [VERIFIED]: 44 groups (29 public, 15 Himalayas) sent in 5 calls, no failure; `Jobs` holds 44 records read back through the connector. **Audited** the same day, `d8f3658` to `f864053`: no live defect, nine findings, all fixed; `2026-09-23-the-projection.md`, fourth section. | **Live test run 35917058573, 2026-09-23T20:36Z** [VERIFIED]: 54 sent in 6 calls, no failure; the operator's four hand-set `Status` values survived with their `Classified at` times unchanged, two of them on rows the run re-sent; `Jobs test` 56 to 59, the three being new postings, with no duplicate `Identity`; `Jobs` still 0; the month so far read back as 6. What remains of the writer is step 3, the star, and the sweep, each its own brief. **Live test run 35912554579, 2026-09-23T19:55Z** [VERIFIED]: 371 rows read, 361 admitted, 56 groups, 56 sent in 6 calls, no failure; `Jobs test` holds 56 records read back through the connector and `Jobs` holds 0. The second live check, a hand-set `Status` surviving another test run, is outstanding. `Title` now carries the normalised title, the operator's decision. **Brief 6, 2026-09-23: the projection is built and wired into every committing run, offline-verified only.** [VERIFIED] over `filtered.json` at `data` head `bab0acf`: 345 rows read, 334 admitted, 29 groups, 29 to send, file byte-identical afterwards; the skip both ways and the member case against hand-written stores. The client is now `src/airtable.py`, one upsert verb, exactly ADR-0035's ten fields *(eleven from 2026-09-24: `Family`, ADR-0038's label, classified pipeline-owned on 2026-09-23, created on both tables and sent from G4)*, no read path, test mode bound to `Jobs test`. A failed projection exits 2 after the commit. **This row moves only on the live checks**, which need the operator's push and a test-mode dispatch; see Blocked. *(The text from here to the end of this cell is the step-1 entry, kept as written.)* **Step 1, the client, built 2026-09-23 on the operator's go for step 1 only.** [VERIFIED] `src/airtable_client.py` and the shared `src/resilience.py`, 44 tests, 427 in the suite on Python 3.12 and 3.11; ADR-0034's Confirmation met both ways: the grep half is a test proved able to fail, and breaking the shared backoff once fails tests in both the fetch module and the client. 21 mutations, one survivor closed. **Nothing calls it yet and no call has reached Airtable**, so the run log does not carry its counters and the monthly count starts at zero each run until step 2 reads the month's run logs from both branches. **Step 2, the projection, is blocked** on the two decisions in the Blocked table. *(Before 2026-09-23 this row read UNBLOCKED, held: the token and seven secrets existed from 2026-09-18 and the operator held the work until the seat was restarted.)* What it must do is settled: its own client (ADR-0034); upsert on Identity writing only pipeline-owned fields (ADR-0035); the current chain filters the projection (ADR-0040); **skip any identity already held in an outcome store, so a deleted row never returns** (ADR-0046); aggregator rows to the private base (ADR-0047). It does not need the `Status` choices; the sweep does | 2026-09-23 | ADR-0034, ADR-0035, ADR-0040, ADR-0046, ADR-0047, `2026-09-22-speechify-rotation-and-the-writer-check.md` |
| The writer's constraints assembled in one place | DONE | [VERIFIED] every constraint traced to the record it comes from, and the built state of each dependency checked against the tree: the star, the grouping, the term-to-family map and the chain all exist; the three `Status` choices do not. Written at session close because the constraints span nine records over six days and one of them, ADR-0035's field ownership, silently destroys the operator's classifications if missed | 2026-09-22 | `docs/how-to/build-the-writer.md` |
| Daily sweep, routing to three outcome stores | PENDING, behind the writer and the `Status` choices | Design superseded 2026-09-19 by ADR-0046: the operator sets `Status` in `Jobs`; the daily sweep copies the row to its classification table, then at fifteen days on `Classified at` writes it to its store, reads the store back, and only then deletes it from `Jobs`; the two rejection tables delete at fifteen days on `Classified`; `accepted` is deleted by a tool the operator runs. **[VERIFIED] 2026-09-22 through the connector:** `Classified at` exists on `Jobs` and `Jobs test` watching `Status` alone; the three new `Status` choices do not, and the connector cannot add them, so the operator adds them by hand | 2026-09-22 | ADR-0043, ADR-0046 |
| Contract check | PARTIAL: built, not yet scheduled on GitHub | *(2026-09-24: built. `src/contract.py` and `.github/workflows/contract.yml`, daily at 06:30 UTC. [VERIFIED] offline and against the live boards with `--no-commit`: baseline, then unchanged, then a field removed by hand from the stored fingerprint named with its current shape, exit 0 each time. The first scheduled run records the baseline; the Airtable row ADR-0036 names is not built)* **Daily**, chosen by the operator from the costed options: about 90 requests a month now and about 240 after ADR-0029's five adapters, against a per-run ceiling of 500 that no run has come within 90% of. ADR-0036 settled the channel. Still unbuilt | 2026-09-18 | ADR-0018, ADR-0036 |

## Blocked, and on whom

Current blockers only. Resolved paragraphs moved to `docs/reference/completed.md` verbatim on 2026-09-22.

| Item | Blocked on | Who | Since |
|---|---|---|---|
| The sweep | *(2026-09-24T15:30Z: the rename is done. [VERIFIED] both tables hold exactly `accepted`, `rejected-poor-filtering` and `rejected-not-a-fit`, each under its old choice ID, and `expired_before_review` is gone. All 8 marked rows, 3 in `Jobs` and 5 in `Jobs test`, keep their meaning, and not one `Classified at` moved. So the sweep now waits only on its brief)* *(2026-09-24T15:10Z: D3 answered. A closed posting is stored with the reason "closed", stays visible in `Jobs` for fifteen days after it closed, then goes under the same delete rule; the mechanism is the sweep brief's. [VERIFIED] through the connector: `Status` on both tables still holds the original four, `applied`, `rejected_pipeline`, `rejected_choice` and `expired_before_review`, so ADR-0046's `not fit` and `poor filtering` never existed in the base. `Jobs` has 3 rows marked, all `rejected_pipeline`; `Jobs test` has 5; no row in either holds `expired_before_review`. The operator renames in the browser: `applied` to `accepted`, `rejected_pipeline` to `rejected-poor-filtering`, `rejected_choice` to `rejected-not-a-fit`, and deletes `expired_before_review`)* *(2026-09-24: the three test copies of the classification tables exist, named with a ` test` suffix like `Jobs test`, created through the connector on the operator's D7. Their IDs are not written here, for the same reason as every table ID)* Three `Status` choices on `Jobs` and `Jobs test`: `not fit`, `poor filtering`, `accepted`. The connector cannot add a choice to an existing field. *(2026-09-24: the operator chose names matching the tables instead, `rejected-not-a-fit`, `rejected-poor-filtering` and `accepted`, by renaming the old choices in the browser so rows already marked keep their meaning. Also waiting: his answer on how a closed posting leaves `Jobs`)* | Operator, by hand | 2026-09-20 |
| Rotating `AGGREGATOR_STORE_TOKEN` | It expires on **2027-01-01**, the operator's figure. A run that cannot reach the private store fails its projection and exits 2. *(2026-09-24: the escalation is built and counts a private-store failure, so three such runs in a row turn the run red and an expired token shows within about a day and a half. The run also skips Himalayas while the store is unreachable.)* Rotate it before then | Operator | 2026-09-24 |
| Himalayas' rows being stored at all | *(2026-09-24T15:10Z: the token writes. [VERIFIED] test-mode dispatch 36016481510 at `e2a481d`: `private_store` block `pushed: true`, 3 files, no failure, branch `data-test`; Himalayas fetched 500, new 500, kept 32; the public `data-test` branch holds no Himalayas file, and no stored file on it names Himalayas. Still to see: a second run restoring those 3 files, and production's first morning run for ADR-0047's Confirmation)* *(2026-09-24: the write path is built. What remains is its first live run: a test-mode dispatch to prove the token can write, then the first scheduled morning run for ADR-0047's Confirmation. The token's write permission is unverified; the runs so far only read)* ADR-0047's write path to the private repository, which is unbuilt and not in Brief 6. *(Until 2026-09-23 this row was the repository and its secret. Brief 6 says both `AGGREGATOR_STORE_REPO` and `AGGREGATOR_STORE_TOKEN` now exist; not verified by this seat, which cannot read repository secrets. The projection already reads the private repository's outcome stores)* | A later brief | 2026-09-23 |
| Wiring the star, step 3 | The next brief. It needs the `Star reason` field, classified 2026-09-23 and not yet built, and the accepted store the sweep writes | Architecture chat | 2026-09-23 |
| Himalayas' trial verdict | 2026-09-26. *(Deferred by the operator 2026-09-23 until the writer runs and he has seen rows in `Jobs`; no earlier than 2026-10-07, per `CHAT_STATE.md` item 81)* | Operator | 2026-09-17 |
| Banyan Canopy's review | 2026-09-24. *(Deferred with Himalayas, same date, same source)* | Operator | 2026-09-17 |
| The stated-experience rule | Deferred by the operator until filtering reads descriptions | Operator | 2026-09-17 |

**Decided, not blocked:** the contract check's cadence is daily (ADR-0036), Rozee.pk is deferred with its trigger in `docs/deferred/`, and the `To review` view exists on `Jobs`.

## Known unverified

**The seniority rule drops accented senior words.** *(Fixed 2026-09-24 on the operator's decision: `fold` now strips accents from Latin letters. Before shipping, every one of 1,458 distinct stored titles was folded both ways: 21 folds changed, one verdict changed, this title's, and no two dedupe keys merged.)* [VERIFIED] false, 2026-09-24: production's first projection put a Himalayas posting titled "Fullstack Developer \| Sênior (13593)" into `Jobs`. `fold` normalises with NFKC, which keeps the circumflex, so "sênior" never matches "senior". Whether to strip accents before matching is a rule change and the operator's. `2026-09-23-the-projection.md`, fourth section.

**ADR-0046's corrected budget, and the record cap, with Himalayas in the projection.** *(2026-09-24, production's first projection [VERIFIED]: 5 calls for 44 groups, 29 public and 15 Himalayas, against ADR-0046's 3. At 5 or 6 calls a run the projection is 300 to 360 calls a month and the total about 57% to 63%, still inferred until a week of runs is read.)* *(2026-09-23, after the second test run: the accumulation is now observed, not inferred. [VERIFIED] five of run 1's Himalayas rows were not sent again by run 2 and remain in `Jobs test`, since 56 plus 3 new less 54 sent leaves 5 untouched. The budget figure is still inferred.)* [INFERRED] from one test run: 6 calls for 56 groups, 27 of them Himalayas rows, which ADR-0047 sends to the private base. ADR-0046's three calls a run counted the public file's 29 groups alone, so a production run is likely five or six calls and the total nearer 60% than 45%. And until ADR-0047's write path exists, each run's Himalayas rows live only for that run, so none is ever re-projected or removed and they accumulate in `Jobs` against the base's 1,000-record cap. Measure after a few production runs by reading `Jobs`' count through the connector. For the architecture chat. `2026-09-23-the-projection.md`.

**The projection reaches the base and the private store on a runner.** *(2026-09-23, test run 35912554579 [VERIFIED]: Airtable accepted the upsert and millisecond dates, every canary was matched by `Identity` with no duplicate, and the private repository was reached through git with the token in a Basic header. Still unexercised: `null` for an empty field, since no row sent had one.)* Unexercised. Four things only a live run can show: that Airtable accepts the upsert body and the dates at millisecond precision; that `null` is accepted for an empty field; that a fine-grained token in a Basic header reaches the private repository through git; and that `Jobs test`'s ten seeded canary rows are matched by identity or left alone. Any canary whose identity the test run does not send stays, so `Jobs test`'s row count will read the sent count plus those. `2026-09-23-the-projection.md`.

**Airtable behaves as the client assumes.** *(2026-09-23: the client is now `src/airtable.py`, with upsert only. Airtable's own documentation, read that day, confirms `PATCH` with `performUpsert.fieldsToMergeOn` on all plans, updates only the fields sent, fails when more than one record matches, and gives 5 requests a second per base and a 30-second wait after a 429. The rest of this entry is as written.)* Nothing has called Airtable through `src/airtable_client.py`. Sourced from its documentation through ADR-0004 and ADR-0034, not exercised: the upsert response's `createdRecords` and `updatedRecords`, the delete response's `deleted` flags, the 429 and its 30-second wait, five requests a second per base, and a monthly allowance of 1,000 calls per workspace. Whether calls made through a session's Airtable connector count against the same allowance is unknown. ADR-0035's Confirmation against `Jobs test`, in step 2, is the first exercise. `2026-09-22-speechify-rotation-and-the-writer-check.md`.

**The heredoc guard catches the failure it was built for.** *(2026-09-23: the path defect below is fixed. The hook runs the guard through a launcher that finds it from `CLAUDE_PROJECT_DIR` or by walking up from the working directory, and fails open when it cannot. [VERIFIED] offline, the launcher asks on a risky heredoc and passes a harmless one from a subdirectory, with and without the variable; and live, a Bash call ran with the shell left in `docs/decisions`. The guard's "ask" has still not been seen raised by the harness itself.)* Still never observed. [VERIFIED] 2026-09-23 that the harness invokes it: with the shell left in `docs/decisions`, a Bash call was refused with `python: can't open file '...\docs\decisions\tools\heredoc_guard.py'`. **That is a defect.** The hook passes a path relative to the working directory, Python exits 2 when it cannot open a script, and exit 2 from a PreToolUse hook blocks the call, so the guard fails closed on every Bash call once the shell is left in a subdirectory of the repository. A `cd` outside the repository is reset by the harness and does not trigger it. Not fixed: anchoring the path on `CLAUDE_PROJECT_DIR` is proposed to the operator. `2026-09-22-speechify-rotation-and-the-writer-check.md`.

**The audit seat has never run.** *(Stale since 2026-09-23, found by the audit of 2026-09-24. It has run three times: the audit of `build-the-writer.md` that `8f4c251` records, at `ultracode` with no subagents per that commit's message; the decision-corpus audit at `b242d9a`; and the audit of `d8f3658` to `f864053`. The last two report using no subagents. So `ultracode` is available on the operator's plan, taken from that commit message; the two questions about subagents are still open, because none has been used.)* `docs/how-to/the-seats.md` defines a fourth seat running Claude Code at `ultracode`, which is `xhigh` effort plus dynamic workflows across parallel subagents. Everything that file says about how it behaves is design reasoned from Anthropic's documentation, read 2026-09-22, and not one observation. Three things are unknown and each would change the design: whether the operator's plan allows `ultracode` at all, whether the `PreToolUse` heredoc guard fires on a subagent's `Bash` calls, and whether a workflow's synthesis is accurate enough to act on without re-running its checks. The first job given to it should be scoped small enough that a bad answer is cheap. `docs/how-to/the-seats.md`.

**The workflow's branch fetch and push behave on GitHub as they did in simulation.** [VERIFIED] now in full: run 35236531478 created `data`, run 35239033514 created `data-test`, and scheduled run 35253312417 fetched the existing `data` at depth 1, restored from it, appended, and fast-forwarded the push. `2026-09-17-first-scheduled-run.md`.

**Boards publish to their APIs at the moment a posting goes live.** ADR-0006's cadence rests on it. **Recorded as confirmed 2026-09-11** in ADR-0006's Assumptions, and its Changes row explains why: a posting 0.26 hours old at fetch is positive evidence, and a second run "would tighten the bound and cannot change the verdict". The record outranks this file under ADR-0022. Until 2026-09-17 this entry read "[VERIFIED] partially" and asked for a second run; it predated the record's amendment and was stale. `2026-09-11-endpoint-feasibility-spike.md`, ADR-0006.

**Many ATS platforms expose no publication date.** ADR-0007 asserts it. **Now settled across 16 platforms and it holds, narrowly.** [VERIFIED] a publication date exists on Greenhouse, Lever, Himalayas, Ashby, Workable, SmartRecruiters, Breezy, Pinpoint and BambooHR, and on JazzHR, Freshteam, Zoho and iCIMS only inside per-posting HTML. [VERIFIED] **Manatal exposes no date field of any kind**, across 2 boards and 34 postings, and it holds 8 registry boards. Dover's per-employer board carries none either. Manatal rows can never satisfy Measure A and must use ADR-0007's first-seen fallback. ADR-0007 is unrevised. `2026-09-16-publication-date-across-untested-platforms.md`.

**Workday `startDate` means publication.** [VERIFIED] behaviourally: it equals the fetch date minus the relative age in `postedOn` on 7 of 7 postings spanning ages 1 to 13 days, and none is in the future. An employment start date would not track posting age. The field name still does not say what it holds, so provenance must be recorded. `2026-09-16-second-observation-checks.md`.

**iCIMS `datePosted` is generated, not real.** [VERIFIED] false. The earlier suspicion is **withdrawn**: an older posting reports `2025-05-15T04:00:00.000Z`, sixteen months before the three that shared `2026-09-10T04:00:00.000Z`. The field varies per posting. The shared `04:00:00.000Z` is midnight US Eastern, so the value is a date with no time. `2026-09-16-second-observation-checks.md`.

**Measure A's coverage on the slice is 100%.** [VERIFIED] every one of 916 Greenhouse and Lever rows and all 500 Himalayas rows carries a real publication date; nothing falls back to first-seen. 43 Lever rows carry the date with its meaning unconfirmed. `2026-09-17-vertical-slice.md`.

**ADR-0028's ceiling of 500 is forty times the observed need.** [VERIFIED] first four observations: 11 requests for the eleven ATS boards, 36 on first contact with Himalayas, 12 in steady state for the whole slice. That is one month short of the evidence the record's Confirmation asks for, and nothing yet reads the run logs to aggregate it. `2026-09-17-vertical-slice.md`. **2026-09-22, architecture chat:** [VERIFIED] 36 requests in each of eleven production runs, read from the run logs on `data`.

**The title pool admits 2.1% of postings, and 40 of its 50 terms admitted nothing.** [VERIFIED] 19 rows from 916, seven of them Pakistan-reachable. One concrete gap found: "Forward Deployment Engineer" is dropped while "Senior Forward Deployed Engineer" is kept, because ADR-0021's plural rule cannot reach "Deployment" from `forward deployed`. Widening the pool is the operator's call. `2026-09-17-vertical-slice.md`. **Updated 2026-09-17 against production data:** 21 of 796 admitted (2.6%), 10 terms credited, 39 matching nothing. The operator decided to keep all 39, and the measurement is recorded in `docs/reference/title-pool.md`. `tools/title_pool_report.py` re-measures it and previews candidates: on these eleven boards almost every AI candidate term matched nothing, so widening the AI side depends on adding boards more than terms. `2026-09-17-operator-decisions-d1-to-d5.md`.

**Lever `createdAt` means published.** Still open. [VERIFIED] not contradicted: across five days both Lever boards produced one new posting, whose `createdAt` postdates the baseline clock, so 0 of 1 newly visible postings predate it. One appearance cannot establish the field's meaning. If Lever enters the slice, twice-daily polling answers this from the pipeline's own data within days. `2026-09-16-second-observation-checks.md`. `tools/publication_lag_report.py` now answers it from the data branch once a second production run exists: a Lever posting first seen in a later run and dated before the run that missed it is the evidence. [VERIFIED] 2026-09-17: one production run so far, so nothing to report yet. **2026-09-17, second production run:** 1 of 1 Lever posting first seen in run 2 is dated after run 1, as are 7 of 7 Greenhouse postings. Consistent, and one posting is not proof. **2026-09-18, third run:** now 2 of 2 Lever and 11 of 11 Greenhouse, with none dated at or before the run that missed it. Still consistent, still not proof; two postings is a second observation, not a demonstration of what the field means. `2026-09-17-first-scheduled-run.md`.

**Speechify's board changes by closing whole roles and rotating the cities of the rest, not by a fault.** [VERIFIED] 2026-09-11 to 2026-09-16, 1086 to 361: 723 of the 813 lost postings were four closed roles. [VERIFIED] 2026-09-16 to 2026-09-17T14:52Z, 361 to 255: four roles closed (106 postings) and 64 location copies rotated out and back in; 191 postings were in both snapshots. [INFERRED] the 191 reported at 03:43Z is that intersection, before the 64 replacements appeared; the count matches exactly, the ids were discarded. `2026-09-17-first-github-runs.md`. **Second rotation, 2026-09-22:** [VERIFIED] 256 to 192 to 254 across three runs; 64 identities last seen at 2026-09-21T18:32Z, 62 new at 2026-09-22T17:33Z, none of them a returning copy, their publication dates from 2024-01-24 to 2026-05-21. [INFERRED] a run landing mid-swap sees the intersection, as the 03:43Z run did on 2026-09-17. The cadence is unknown from two events. `2026-09-22-speechify-rotation-and-the-writer-check.md`.

**Cross-source duplicates collapse.** [VERIFIED] false, 2026-09-24: "GTM AI Engineer -Deal Desk" is in `Jobs` twice, both rows reading employer "Motive", one from Motive's Greenhouse board published 2026-09-19T03:03Z and one through Himalayas dated 2026-09-23T23:07Z. ADR-0001's key includes the publication date, and Himalayas stamps its own, so an aggregator's copy never shares a key with the original. For the architecture chat. `2026-09-24-operator-decisions-and-the-next-builds.md`.

**Scheduled runs keep running.** *(Re-read 2026-09-24 on GitHub's page for disabling workflows: it still does not say what counts as repository activity.)* [VERIFIED] from GitHub's documentation: "In a public repository, scheduled workflows are automatically disabled when no repository activity has occurred in 60 days." Whether the pipeline's own data-branch pushes count as activity is not established. Already recorded as a risk in `docs/architecture-2.0.md`, with the operator's LinkedIn pipeline running over 100 times on bot commits alone as evidence against it. `2026-09-17-run-log-reader-and-speechify.md`.

**Postings per run are on the order of one thousand.** ADR-0001:27, inherited by ADR-0003:28. [VERIFIED] false: 11 of the 53 boards return 1646 postings. Median 27 per board, maximum 1086. The 53-board total is unmeasured. `2026-09-15-spike-followup-checks.md`.

**Lever `createdAt` means published.** Measure A on Lever rows depends on it. [VERIFIED] not established: hosted pages display no date. Page-source JSON-LD `datePosted` matches `createdAt`'s UTC date on 3 of 3, but both come from Lever, so the match is not independent. `2026-09-15-spike-followup-checks.md`.

**Greenhouse `updated_at` marks an edit to a posting.** [VERIFIED] unreliable as such: it is bulk-stamped. 16 of 21 Careem postings share one instant, and Speechify's run in four rotating batches of about 265. What writes it is unknown. `2026-09-15-spike-followup-checks.md`.

**Himalayas can be read newest-first and stopped early.** [VERIFIED] partially: the browse endpoint orders by `pubDate` and paginates by cursor without duplicates over 3 pages. The search endpoint does neither. Browse's newest posting trailed search's by 97.7 minutes. [BELIEVED] from that lag, not observed: a stopping rule keyed to a run's wall clock would skip postings that reach browse late. Termination is not established. `2026-09-15-spike-followup-checks.md`.
